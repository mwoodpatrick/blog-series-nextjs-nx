import './server-side.module.css';

/* eslint-disable-next-line */
export interface ServerSideProps {}

export function ServerSide(props: ServerSideProps) {
  return (
    <div>
      <h1>Welcome to ServerSide!</h1>
    </div>
  );
}

export default ServerSide;
